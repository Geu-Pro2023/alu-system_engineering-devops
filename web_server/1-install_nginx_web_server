#!/usr/bin/env bash
# installs the reverse proxy - Nginx
sudo apt update && sudo apt upgrade -y  # Corrected the upgrade command
sudo apt install nginx -y 
echo "Holberton School" | sudo tee /var/www/html/index.html
sudo service nginx start

